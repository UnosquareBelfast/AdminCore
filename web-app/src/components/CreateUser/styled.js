import styled from 'styled-components';

export const Container = styled.div`
  label {
    display: block;
  }
  input,
  select {
    display: block;
    margin: 0.2rem 0 1rem 0;
    width: 90%;
    height: 1.5rem;
    padding: 0 0.5rem;
  }
`;
