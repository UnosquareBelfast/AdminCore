import React from 'react';
import { SpinnerContainer } from './styled';

const Spinner = () => {
  return (
    <SpinnerContainer>
      <div />
      <div />
    </SpinnerContainer>
  );
};

export default Spinner;
