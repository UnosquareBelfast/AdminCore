import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  card: {
    paddingTop: 20,
    borderWidth: 1,
    borderColor: '#fff',
    marginHorizontal: 20,
    paddingLeft: 20,
    paddingRight: 20,
    backgroundColor: '#fff',
    borderRadius: 15,
  },
});

export default styles;
